module <%= class_name %>

  OPTIONS = [<%= args.map {|a| ":#{a}"}.join(", ") %>]

  module Defaults

  end

  include ClassyEnum
end
<% args.each do |arg| %>
class <%= class_name + arg.camelize %>

end
<% end %> 
