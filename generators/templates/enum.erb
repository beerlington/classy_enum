module <%= class_name %>
  OPTIONS = [<%= args.map {|a| ":#{a}"}.join(", ") %>]

  module InstanceMethods
  end

  module ClassMethods
  end

  include ClassyEnum
end
<% args.each do |arg| %>
class <%= class_name + arg.camelize %>
end
<% end %> 
